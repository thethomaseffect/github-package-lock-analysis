{
  "name": "sample-project",
  "version": "1.0.0",
  "private": true,
  "description": "Fixture project for lockfile analysis — do not npm install in CI",
  "dependencies": {
    "accepts": "^1.3.8",
    "cheerio": "0.22.0"
  }
}
